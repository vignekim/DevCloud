<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.folder.app.auth.AuthMapper">

<select id="findById" parameterType="String" resultType="com.folder.app.auth.User">
  SELECT * FROM USER WHERE name = #{name}
</select>

</mapper>